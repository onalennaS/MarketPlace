# TODO

- [ ] Update `Market/settings.py` to remove requirement for `SECRET_KEY` from environment for local runs.
- [ ] Ensure any Render/production-only config is not required locally (keep local-friendly defaults).
- [ ] Run `python manage.py runserver` (and/or `python manage.py check`) to verify Django starts.

